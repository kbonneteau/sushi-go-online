const cardModel = require("../models/cardModel");

let cardController = {
    /**
     * Grabs data related to the card type specified by the client. Returns the card to the user
    */
    // getSinglecard: (req, res) => {
    //     const card = cardModel.readcards().find(card => card.cardType === req.params.cardType);
    //     card ? res.status(200).json(card) : res.status(404).json({ error: "Unable to find a card with the provided name." });
    // }

    
}

module.exports = cardController;